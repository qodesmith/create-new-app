{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Indicates to TypeScript that this "project" can be referenced by other
    // "projects". This is required because the server and client tsconfigs
    // reference this tsconfig.
    "composite": true
  },
  "include": ["./**/*"]
}
